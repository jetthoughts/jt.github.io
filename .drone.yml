kind: pipeline
type: kubernetes
name: default

clone:
  depth: 1

steps:
- name: build
  image: circleci/ruby:2.6.5-node-browsers
  commands:
  - sudo chmod -R 775 .git
  - bin/setup
  - bin/build
  - .circleci/setup-github

trigger:
  branch:
  - develop
  - drone-io
