/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: Sep 13 2022 | 10:35:25 */
(function ($) {
    if (!window.AV) {
        AV = {};
    }
    /*
     * Application Init, everything starts here
     *
     */
    AV.App = function() {
        return {
            exists: function(x) {
                if ($(x).length > 0) { return true; }
            },
            init: function() {				
              	// Clone Social Icons to Mobile Menu, Add link to menu logo
				if(AV.App.exists('nav.pp-menu-nav.pp-off-canvas-menu.pp-menu-right')){
					$(".uk-footer-social .pp-social-icons").clone().appendTo("nav.pp-menu-nav.pp-off-canvas-menu.pp-menu-right");
				}     
				if(AV.App.exists('.uk-slider')){
					if($(window).width() > 861) {
						$(".ukmform").appendTo(".uk-slider .bx-wrapper");
					}
					if($(window).width() < 860) {
						var hero = $(".uk-slider .bx-wrapper img").height()-40;
						$(".uk-slider .bx-wrapper .bx-pager").css('top', hero+'px');
					}
				}
				if(AV.App.exists('.post-archive-filter')){
					var elem = $('.uk-search .pp-search-form-wrap').detach();
					$('.post-archive-filter .pp-post-filters-wrapper').append(elem);
				}
				// Modify Search Text				
				if(AV.App.exists('.search-results')){
					$(".search-results h3.fl-heading .fl-heading-text").text($(".search-results h3.fl-heading .fl-heading-text").text().replace("Search Results: ", ""));
					$(".search-results h3.fl-heading .fl-heading-text").text($(".search-results h3.fl-heading .fl-heading-text").text().replace("Showing ", ""));
				}
				// Basic Clickable on Whole Column
				if(AV.App.exists('.service-clickable')){
					$(document).on('click', 'body:not(.fl-builder-edit) .service-clickable .fl-row-content', function(event) { 
                        window.location = $(this).find("a").attr("href"); 
                      	return false;
                    });
				}
				// Handle the Gravity Form               	
              	if(AV.App.exists('.gform_body.gform-body')){
                  $(document).ready(function(){
                      $(".gfield_error input, .ginput_container input, .ginput_container textarea, .input-wrap input").focus(function(){
                          $(this).parent().parent().addClass('active');
						  $(this).parent().prev('label').addClass('label-focus');
                      }).blur(function(){
						  $(this).parent().prev('label').removeClass('label-focus');
                          var cval = $(this).val();
                          if(cval.length < 1) {
                              $(this).parent().parent().removeClass('active');

                          }
                      });
                  });
                }
				// Handle the Custom Form               	
              	if(AV.App.exists('form.ukmform')){
                  $(document).ready(function(){
                      $(".input-wrap input").focus(function(){
                          $(this).parent().addClass('active');
						  $(this).prev('label').addClass('label-focus');
                      }).blur(function(){
						  $(this).prev('label').removeClass('label-focus');
                          var cval = $(this).val();
                          if(cval.length < 1) {
                              $(this).parent().removeClass('active');

                          }
                      });
                  });
                }
				// Disable Button on Agency Options
				if(AV.App.exists('.agency-options')){
					$('.agency-options a.fl-button').click(function(e) {
						e.preventDefault();
					});
				}
				// Sidebar Active
				if(AV.App.exists('.uk-toc')){
					// Cache selectors
					var lastId,
						topMenu = $(".uk-toc"),
						topMenuHeight = $("header.fl-builder-content").outerHeight(),//topMenu.outerHeight()-250,
						// All list items
						menuItems = topMenu.find("a"),
						// Anchors corresponding to menu items
						scrollItems = menuItems.map(function(){
						  var item = $($(this).attr("href"));
						  if (item.length) { return item; }
						});

					// Bind click handler to menu items
					// so we can get a fancy scroll animation
					menuItems.click(function(e){
					  var href = $(this).attr("href"),
						  offsetTop = href === "#" ? 0 : $(href).offset().top-topMenuHeight+1;
					  $('html, body').stop().animate({ 
						  scrollTop: offsetTop
					  }, 800);
					  e.preventDefault();
					});

					// Bind to scroll
					$(window).scroll(function(){
					   // Get container scroll position
					   var fromTop = $(this).scrollTop()+topMenuHeight;

						// Get id of current scroll item
					   var cur = scrollItems.map(function(){
						 if ($(this).offset().top < fromTop)
						   return this;
					   });
					   // Get the id of the current element
					   cur = cur[cur.length-1];
					   var id = cur && cur.length ? cur[0].id : "";

					   if (lastId !== id) {
						   lastId = id;
						   // Set/remove active class
						   menuItems
							 .parent().removeClass("active")
							 .end().filter("[href='#"+id+"']").parent().addClass("active");
					   }                   
					});
				}
            } // CLOSE INIT FUNCTION
        };
    }();
  
 
    $(function() {
        AV.App.init();
    });
}(jQuery));
